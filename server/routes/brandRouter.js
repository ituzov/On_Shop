const router = require('express').Router();


router.post('/',(req, res) => {
    // обработчик для POST-запроса на /device
});
router.get('/',(req, res) => {
    // обработчик для POST-запроса на /device
});


module.exports = router;